import { DonationHistoryDTO } from './donation-history-dto';

describe('DonationHistoryDTO', () => {
  it('should create an instance', () => {
    expect(new DonationHistoryDTO()).toBeTruthy();
  });
});
